# VNC Configuration
# Display number (creates :1)
DISPLAY_NUM=1

# VNC geometry
GEOMETRY=1024x768

# VNC password file
VNC_PASSWD_FILE=~/.vnc/passwd

# Websockify port for noVNC
WEBSOCK_PORT=5000

# VNC server port (5901 for display :1)
VNC_PORT=5901

# Desktop session
DESKTOP_SESSION=fluxbox